// 01. Create a new file named "ts.ts" in the src folder of CodingProject.
// 02. Write the following code into that file:

// 03. In this code, we have defined a function called "addTwoNumbers".

function addTwoNumbers(a: number, b: number): number {
    return a + b;
}

// 04. Call the function and pass it two numbers as arguments.
let result = addTwoNumbers(2, 3);

console.log(result); // Output: 5
