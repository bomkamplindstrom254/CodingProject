// This is an example TypeScript code.
// It defines two variables: `value` and `message`.

let value = 10;
let message = "Hello, World!";

console.log(`The value of ` + value + ` is ${value}.`);
console.log(`The message is: ${message}`);
